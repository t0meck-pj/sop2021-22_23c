Zadanie 1.1

#include <stdio.h>
#include <stdlib.h>

int main() {
  int liczbaNumerow; 
  int wynik = 0;
  int numer = 0;

  printf("Podaj liczbe numerow: ");
  scanf("%d", liczbaNumerow);

  for (i = 1; i <= liczbaNumerow; ++i) {
    printf("Podaj liczbe %d: ", i);
    scanf("%d", &numer);

    wynik = wynik + numer;
  }

  printf("Suma  = %d\n", wynik);
  
  return 0;
}

##################################

Zadanie 1.2

#include <stdio.h>
#include <stdlib.h>

int main() {
  int liczbaNumerow;
  int wynik = 0;
  int numer = 0;

  printf("Podaj liczbe numerow: ");
  scanf("%d", liczbaNumerow);

  for (i = 1; i <= liczbaNumerow; ++i) {
    printf("Podaj liczbe %d: ", i);
    scanf("%d", &numer);

    wynik = wynik + numer;
  }

 return printf("Suma  = %d\n", wynik);
}

#################################

Zadanie 1.3

Ahhh, moja pieta Ahillesowa, pozwolilem sobie najpierw zrobic ja w Javie(chociaż i tam były problemy), a dopiero później "przetłumaczyc na C", jednak nadal to nie wyszlo 
(problem ze zrozumieniem konstrukcji choinki...) , wiec zdecydowalem sie na c++, chociaz i tutaj musialem sie podeprzec internetem ... (przepraszam postaram sie to zrozumiec, ale 
akurat choinka z gwiazdek spedza mi sen z powiek od samego poczatku)

#include <iostream>

using namespace std; int main() {
    int n;
    char znak = '*';
    cout<<"Podaj liczbe wierszy\n";
    cin>>n;
    char *line=new char[2*n-1];
    for(int i=0;i<2*n-1;i++){
    line[i]=znak;
    }
    for(int j=0;j<n;j++)
    {
        for(int i=0;i<n-j;i++)
        cout<<' ';
        for(int i=0;i<2*j-1;i++)
        cout<<line[i];
        cout<<endl;
    }
    return 0;
}

################################

Zadanie 
